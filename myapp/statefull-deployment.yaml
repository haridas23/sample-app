apiVersion: v1
kind: Service
metadata:
  name: statefull-sample-app
  labels:
    app: statefull-sample-app
spec:
  clusterIP: None   # Headless service required for StatefulSet
  ports:
    - port: 80
      name: http
  selector:
    app: statefull-sample-app
---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: statefull-sample-app
spec:
  serviceName: "statefull-sample-app"
  replicas: 3
  selector:
    matchLabels:
      app: statefull-sample-app
  template:
    metadata:
      labels:
        app: statefull-sample-app
    spec:
      containers:
        - name: statefull-sample-app
          image: nginx:stable
          ports:
            - containerPort: 3000
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /
              port: http
            initialDelaySeconds: 15
            periodSeconds: 20
          volumeMounts:
            - name: data
              mountPath: /usr/share/nginx/html
  volumeClaimTemplates:
    - metadata:
        name: data
      spec:
        accessModes: ["ReadWriteOnce"]
        storageClassName: "standard"  # GKE default storage class
        resources:
          requests:
            storage: 5Gi
