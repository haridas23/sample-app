apiVersion: apps/v1
kind: Deployment
metadata:
  name: canary-sample-app
  labels:
    app: canary-sample-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: canary-sample-app
  template:
    metadata:
      labels:
        app: canary-sample-app
    spec:
      containers:
        - name: canary-my-app
          image: asia-south1-docker.pkg.dev/s-0-009988/sample-app/my-app-canary:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3000
          resources:
            requests:
              cpu: "100m"
              memory: "256Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"

---
apiVersion: v1
kind: Service
metadata:
  name: canary-sample-app-ilb
  annotations:
    cloud.google.com/backend-config: '{"default": "canary-sample-app-backendconfig"}'
    cloud.google.com/neg: '{"exposed_ports": {"80":{"canary-sample-app": "canary-sample-app-80"}}}'  
spec:
  type: ClusterIP
  selector:
    app: canary-sample-app
  ports:
    - name: http
      port: 80
      targetPort: 3000
